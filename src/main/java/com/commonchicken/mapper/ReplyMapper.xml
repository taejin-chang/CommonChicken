<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chicken.mapper.ReplyMapper">
	<!-- parameterType 속성 생략 가능 -->
	<insert id="insertReply">
		<selectKey resultType="int" keyProperty="rplNum" order="BEFORE">
			select reply_tb_seq.nextval from dual
		</selectKey>
		insert into reply_tb values(#{rplNum}, #{revNum}, #{ordDetailNum}, #{memEmail2}, #{cmNum}, #{stoNum}, #{memEmail}, #{rplContent}, #{rplDate})
	</insert>
	
	<select id="selectReplyList" resultType="ReplyDTO">
		select * from reply_tb order by rplnum
	</select>
</mapper>