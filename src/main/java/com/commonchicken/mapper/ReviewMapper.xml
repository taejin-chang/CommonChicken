<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chicken.mapper.ReviewMapper">
	<!-- parameterType 속성 생략 가능 -->
	<insert id="insertReview">
		<selectKey resultType="int" keyProperty="revNum" order="BEFORE">
			select review_tb_seq.nextval from dual
		</selectKey>
		insert into review_tb values(#{revNum}, #{revNum}, #{ordDetailNum}, #{cmNum}, #{revContent}, #{revImg1}, #{revRated}, #{revDate}, #{revViews})
	</insert>
	
	<select id="selectReviewList" resultType="ReviewDTO">
		select * from review_tb order by revNum
	</select>
</mapper>